-- assertions/transactions_cleaned_user_id_not_null.sqlx
config {
  type: "assertion",
  dependencies: ["transactions_cleaned"]
}

SELECT
  COUNT(*)
FROM
  ${ref("transactions_cleaned")}
WHERE
  user_id IS NULL
HAVING
  COUNT(*) > 0